include:
- project: 'platform-admins/shared-ci-cd'
  file: 'cd/gke-deploy.yaml'

deploy-staging:
  tags:
  - cluster:staging, app:APP_NAME

deploy-prod-central:
  tags:
  - cluster:prod-central, app:APP_NAME

deploy-prod-east:
  tags:
  - cluster:prod-east, app:APP_NAME

stages:
  - deploy-staging
  - deploy-prod-central
  - deploy-prod-east
